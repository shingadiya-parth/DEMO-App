package com.example.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Centralized Color Design Tokens for PlayRewards.
 * Friendly, colorful, playful, modern, trustworthy, and reward-focused.
 */
object AppColors {
    // Primary & Action Tokens
    val Primary = Color(0xFF2563EB) // Bright Blue for primary actions
    val PrimaryDark = Color(0xFF1D4ED8)
    val PrimaryLight = Color(0xFFDBEAFE) // Soft ice blue tint
    val PrimaryContainer = Color(0xFFEFF6FF)

    // Deep Navy & Neutral Text Tokens
    val TextNavy = Color(0xFF0F172A) // Deep Navy for primary readable text & titles
    val TextSecondary = Color(0xFF475569) // Slate secondary text
    val TextMuted = Color(0xFF94A3B8) // Muted captions & borders
    val TextOnPrimary = Color(0xFFFFFFFF)

    // Background & Pastel Surface Tokens
    val BackgroundLight = Color(0xFFF8FAFC) // Very light pastel blue/white background
    val SurfaceLight = Color(0xFFFFFFFF) // Crisp white card surface
    val SurfaceVariant = Color(0xFFF1F5F9) // Subtle background card container
    val SurfaceBorder = Color(0xFFE2E8F0) // Clean crisp border line
    val CardBackground = Color(0xFFFFFFFF)

    // Supporting Brand & Feature Colors
    val GoldCoin = Color(0xFFF59E0B) // Rich golden amber for coins
    val GoldCoinLight = Color(0xFFFEF3C7) // Soft gold highlight container
    val GoldCoinDark = Color(0xFFD97706)
    val GoldLight = Color(0xFFFEF3C7)
    val GoldDark = Color(0xFFD97706)

    val SuccessGreen = Color(0xFF10B981) // Vibrant emerald for rewards & check-ins
    val SuccessGreenLight = Color(0xFFD1FAE5) // Soft mint green background
    val SuccessGreenDark = Color(0xFF047857)
    val EmeraldGreen = Color(0xFF10B981)
    val EmeraldLight = Color(0xFFD1FAE5)

    val AccentPurple = Color(0xFF8B5CF6) // Royal violet for special/selected activities
    val AccentPurpleLight = Color(0xFFEDE9FE)
    val AccentPurpleDark = Color(0xFF6D28D9)

    val ActionOrange = Color(0xFFF97316) // Energetic coral orange for hot actions
    val ActionOrangeLight = Color(0xFFFFEDD5)
    val ActionOrangeDark = Color(0xFFC2410C)
    val CoralRed = Color(0xFFEF4444)
    val CoralLight = Color(0xFFFEE2E2)

    val ErrorRed = Color(0xFFEF4444) // Error & destructive actions
    val ErrorRedLight = Color(0xFFFEE2E2)

    // Navy Cards
    val NavyCard = Color(0xFF1E293B)

    // Dark Mode Accents
    val DarkBackground = Color(0xFF0B1120)
    val DarkSurface = Color(0xFF131D33)
    val DarkSurfaceVariant = Color(0xFF1E293B)
    val DarkSurfaceBorder = Color(0xFF334155)
    val DarkTextPrimary = Color(0xFFF8FAFC)
    val DarkTextSecondary = Color(0xFF94A3B8)
}
